# Tiny Tapeout project information
project:
  title:        "Silicon Characterization Instrument"
  author:       "randomuzbek"
  discord:      ""      # Your discord username, for communication and automatically assigning you a Tapeout role (optional)
  description:  "Metastability characterizer: on-chip ring-osc async data + swept delay line + dual-sample witness FFs; raw UART stream for tau/W crowd dataset"
  language:     "Verilog" # other examples include SystemVerilog, Amaranth, VHDL, etc
  clock_hz:     25000000 # Clock frequency in Hz (or 0 if not applicable)

  # How many tiles your design occupies? A single tile is about 167x108 uM.
  tiles: "1x1"          # Valid values: 1x1, 1x2, 2x2, 3x2, 4x2, 6x2 or 8x2

  # Your top module name must start with "tt_um_". Make it unique by including your github username:
  top_module:  "tt_um_randomuzbek_charinst"

  # List your project's source files here.
  # Source files must be in ./src and you must list each source file separately, one per line.
  # Don't forget to also update `PROJECT_SOURCES` in test/Makefile.
  source_files:
    - "project.v"
    - "uart_tx.v"
    - "uart_packet.v"
    - "sweep_ctrl.v"
    - "delay_line.v"
    - "dut_ff_bank.v"
    - "witness_bank.v"
    - "metastable_witness.v"
    - "ring_osc.v"

# The pinout of your project. Leave unused pins blank. DO NOT delete or add any pins.
# This section is for the datasheet/website. Use descriptive names (e.g., RX, TX, MOSI, SCL, SEG_A, etc.).
pinout:
  # Inputs
  ui[0]: "start"
  ui[1]: "mode (0=shmoo,1=mtbf)"
  ui[2]: "ro_div0"
  ui[3]: "ro_div1"
  ui[4]: "ro_div2"
  ui[5]: "dut_sel0"
  ui[6]: "dut_sel1"
  ui[7]: "ext_data"

  # Outputs
  uo[0]: "UART TX"
  uo[1]: "busy"
  uo[2]: "done"
  uo[3]: "heartbeat"
  uo[4]: "tap_dbg0"
  uo[5]: "tap_dbg1"
  uo[6]: "tap_dbg2"
  uo[7]: "tap_dbg3"

  # Bidirectional pins
  uio[0]: "ext_data in (ui[7]=1)"
  uio[1]: "ro_clk out (Fd monitor)"
  uio[2]: ""
  uio[3]: ""
  uio[4]: ""
  uio[5]: ""
  uio[6]: ""
  uio[7]: ""

# Do not change!
yaml_version: 6
